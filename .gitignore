.idea/
.vscode/
__pycache__/
.DS_Store
.rossum-cache/
.test-runs/

# Session-local planning and design notes — not repo artifacts.
# Per-session work (brainstorming specs, implementation plans) lives in the
# user's Claude Code session directory, never in the shared repo tree.
docs/superpowers/
docs/VISION.md
