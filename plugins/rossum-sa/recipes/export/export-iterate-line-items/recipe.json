{
  "name": "export-iterate-line-items",
  "axis": "export",
  "summary": "For each line item, POST the item's fields to an API with a sequence index.",
  "maturity": "standard",
  "params": {
    "line_field":  {"required": true, "description": "multivalue (table) field to iterate"},
    "item_fields": {"required": true, "description": "comma-separated per-line fields to send"},
    "target_url":  {"required": true, "description": "endpoint (templated with «sequence»)"}
  },
  "produces": [],
  "consumes": [],
  "provenance": "export-pipeline-reference / reference.md (Iteration Over Lists)",
  "reference": "export-pipeline-reference#iteration",
  "notes": "The «sequence» token is supplied per-iteration by the engine; it is a declared param so it must appear in the fragment."
}
