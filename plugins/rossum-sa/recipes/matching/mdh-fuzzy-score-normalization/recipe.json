{
  "name": "mdh-fuzzy-score-normalization",
  "axis": "matching",
  "summary": "Fuzzy name search with length-ratio score normalization to penalize length mismatch.",
  "maturity": "standard",
  "params": {
    "dataset":      {"required": true, "description": "MDH collection"},
    "search_field": {"required": true, "description": "fuzzy text field"},
    "threshold":    {"default": 0.4, "description": "normalized score floor (0-1); bounded ~0.5 at raw~1, so default is permissive — calibrate, see README"}
  },
  "produces": ["matched_id", "match_score"],
  "consumes": [],
  "provenance": "mdh-reference / mdh-matching-queries.md (Score Normalization Pattern)",
  "reference": "mdh-reference#matching-queries",
  "notes": "Normalized score is bounded by x/(1+x) (~0.5 at raw~1); the default floor is permissive — calibrate per collection."
}
